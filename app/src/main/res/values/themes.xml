<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AirFlyTravel" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.AirFlyTravel" parent="Base.Theme.AirFlyTravel" />
    <style name="Theme.AirFlyTravel.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">
            @color/splash_background
        </item>

        <item name="windowSplashScreenAnimatedIcon">
            @drawable/ic_splash_logo
        </item>

        <item name="postSplashScreenTheme">
            @style/Theme.AirFlyTravel
        </item>
    </style>

    <style name="RoundedCornersDialog" parent="Theme.Material3.Light.Dialog">
        <item name="shapeAppearanceOverlay">@style/ButtonCorners</item>
    </style>

    <style name="ButtonCorners" parent="">
        <item name="cornerSize">50dp</item> </style>
</resources>